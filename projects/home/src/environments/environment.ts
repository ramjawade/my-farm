/**
 * Development environment. `environment.prod.ts` replaces this in production
 * builds (see angular.json fileReplacements); CI stamps the placeholders.
 */
export const environment = {
  production: false,
  appVersion: '0.1.0',
  buildStamp: 'dev',
  openWeatherApiKey: '',
};
